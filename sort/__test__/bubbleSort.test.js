const bubbleSort = require('../bubbleSort')
const testSort = require('../testSort')

// describe('BubbleSort', () => {
//   it('should sort array', () => {
//     testSort(bubbleSort)
//   })
// })
test('BubbleSort', () => {
  testSort(bubbleSort)
})